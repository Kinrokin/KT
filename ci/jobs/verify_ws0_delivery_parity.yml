name: verify_ws0_delivery_parity
on:
  workflow_call:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate job declaration
        run: python -m tools.operator.program_catalog_verify --check-job ci.ws0.delivery_parity
