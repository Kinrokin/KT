name: verify_program_catalog
on:
  workflow_call:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify program catalog
        run: python -m tools.operator.program_catalog_verify --strict
