# Future Compute Packet Spec

After repo-side gates pass, emit `ktg3_v3.zip`.

Requirements: short ZIP, one-cell runner, HF upload, small assessment ZIP, no manual safetensors upload, no nested notebook maze, no fake PASS receipts.
