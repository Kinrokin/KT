{
  "schema": "MULTIVERSE_EVAL_SCHEMA_V1",
  "schema_version": 1,
  "event_schema": "MULTIVERSE_EVAL_V1",
  "required_fields": [
    "schema",
    "schema_version",
    "fork_root",
    "branches",
    "status"
  ],
  "notes": "Policy B multiverse evaluation schema (stub; fail-closed if invoked without inputs)."
}
