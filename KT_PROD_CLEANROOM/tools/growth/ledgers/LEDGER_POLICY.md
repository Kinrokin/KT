# Ledgers Policy

`KT_PROD_CLEANROOM/tools/growth/ledgers/` contains append-only JSONL ledgers produced by local runs.

This directory is intentionally **empty in git** to avoid committing run artifacts and environment-specific state.

Ledgers are generated after clone.

