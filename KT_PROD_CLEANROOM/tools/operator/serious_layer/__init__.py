"""Serious Layer (operator-local, unbound schemas).

Authority-grade Red Assault + Continuous Governance programs that are:
- generator-first
- deterministic
- replay-verifiable (artifact-level)
- fail-closed

This package intentionally avoids embedding dual-use payloads; any sensitive
probe payloads must be hash-referenced only and executed only in gated lanes.
"""

