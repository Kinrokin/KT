# C006 Import Checklist

- Confirm the second-host run used the current-head handoff pack.
- Confirm the second-host run used the current submission template.
- Confirm the host fingerprint differs from the local machine.
- Confirm the result is deterministic.
- Confirm no hidden secret dependency was introduced.
- Confirm the return file names the current repo head.
- Confirm the return file is copied into `KT_PROD_CLEANROOM/reports/imports/`.
