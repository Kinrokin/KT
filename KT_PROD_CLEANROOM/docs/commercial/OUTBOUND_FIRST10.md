# First 10 outbound messages (templates)

These are short, low-commitment messages that sell what KT actually delivers: deterministic, replayable evidence bundles with mechanical acceptance criteria.

Replace bracketed fields: `<NAME>`, `<COMPANY>`, `<ROLE>`, `<VERTICAL>`, `<ONE_SENTENCE_RISK>`.

## 1) Cold email (short)
Subject: Replayable AI governance evidence (no installs, offline)

Hi <NAME> — quick question.

If an auditor/insurer asked you to prove the integrity of your AI system state (and replay it later), do you have a deterministic evidence bundle you can hand them?

We run a fail-closed operator factory that produces: one-line verdict + WORM run artifacts + delivery zip + sha256 + replay wrappers.

If useful, I can share a sample artifact tree and acceptance checklist.

## 2) Cold email (vertical)
Subject: <VERTICAL> AI audit pack (replayable, hash-backed)

Hi <NAME> — I work on a deterministic evaluation factory that outputs audit-grade evidence (not a narrative) for <VERTICAL> AI systems: drift/regression, adversarial probes, and replay wrappers.

Are you the right person to ask about vendor risk / audit readiness for your AI stack?

## 3) LinkedIn connection note
Hi <NAME> — I build replayable, fail-closed AI governance evidence packs (verdict + hashes + replay). Would like to connect.

## 4) First follow-up
Subject: Re: replayable AI evidence

<NAME> — following up once.

If you have a pinned model/adapters release, we can produce a point-in-time certification pack that’s replayable on a clean machine (zip + sha256 + replay scripts). No installs, offline by default.

Worth a 15-minute fit check?

## 5) “Are you doing this already?” message
Subject: Quick benchmark question

When you ship a model or adapter update, do you have a deterministic way to:
1) prove what was evaluated,
2) prove it passed,
3) replay it later,
4) detect drift/regression across time?

If not, we can deliver that as mechanical artifacts.

## 6) Referral ask
Hi <NAME> — who owns AI audit readiness / model release governance at <COMPANY>? I have a short artifact-driven runbook I can share.

## 7) Failure demo offer (safe)
Subject: Want an “audit pack” example?

I can send a sanitized example of what an evidence bundle looks like: run protocol, one-line verdict, delivery manifest, zip sha256, replay wrappers. No proprietary data.

Interested?

## 8) Red Assault upsell angle
Subject: Prompt-injection risk, but measurable

If you’re already doing evals, the gap is often adversarial pressure + failure taxonomy + replay.
We run bounded red-assault packs and deliver a failure library with hashes + replay.

Is adversarial evaluation currently part of your release gate?

## 9) Continuous governance upsell angle
Subject: Drift/regression reports for AI releases

We can turn point-in-time certification into a monthly governance loop: baseline → diffs → drift thresholds → replayable bundles.

If you had to show a quarterly drift summary to an auditor, could you generate it today?

## 10) “Board-ready” angle
Subject: One-line verdict for board/audit packets

Some teams just need a board-ready statement that is mechanically grounded.
Our runs emit a one-line verdict plus a delivery manifest that ties it to pins, hashes, and replay steps.

If you want, tell me your release cadence and I’ll suggest the minimal SKU ladder.

