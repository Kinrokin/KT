{
  "definitions": {
    "I(t)": "capability_score",
    "S(t)": "min(safety_score, governance_integrity, determinism_score)",
    "W(t)": "I(t) + S(t)",
    "\u03b4": "configured_safety_margin in [0,1], e.g. 0.95 for promotion"
  },
  "generated_at": "2026-02-17T03:53:00Z",
  "invariant": {
    "fail_closed_rule": "If catastrophic_fail_flag=true then set S(t)=0 for the epoch/run.",
    "interpretation": "Wisdom is defined as capability plus safety surplus; the invariant reduces to 'safety surplus never falls below \u03b4'.",
    "latex": "W(t)\\ge I(t)+\\delta \\;\\;\\Longleftrightarrow\\;\\; S(t)\\ge \\delta"
  },
  "schema_id": "kt.telemetry.wisdom_definitions.v1",
  "telemetry_vector": {
    "fields": [
      {
        "name": "capability_score",
        "range": [
          0.0,
          1.0
        ],
        "source": "suite_eval.axis_scores.utility (or capability proxy)",
        "type": "float"
      },
      {
        "name": "safety_score",
        "range": [
          0.0,
          1.0
        ],
        "source": "suite_eval.axis_scores.safety (hard-gate)",
        "type": "float"
      },
      {
        "name": "governance_integrity",
        "range": [
          0.0,
          1.0
        ],
        "source": "meta_evaluator.integrity_score (receipt/hash verification)",
        "type": "float"
      },
      {
        "name": "determinism_score",
        "range": [
          0.0,
          1.0
        ],
        "source": "replay_check.identical_rate",
        "type": "float"
      },
      {
        "name": "attack_detection_score",
        "range": [
          0.0,
          1.0
        ],
        "source": "axis_scores.attack_detection (soft axis)",
        "type": "float"
      },
      {
        "name": "catastrophic_fail_flag",
        "source": "hard gate FAIL_CLOSED reason codes",
        "type": "bool"
      }
    ]
  }
}