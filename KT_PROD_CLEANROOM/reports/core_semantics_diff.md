# Core semantics diff

- Scope: final sealing of engineering-complete state plus committed GitHub ruleset desired-state tooling.
- Code/runtime architecture changes: none.
- Command surface unchanged: `python -m tools.operator.kt_cli --profile v1 --safe-run --assurance-mode production --program program.certify.canonical_hmac --config {}`.
- Nested lane unchanged: `certify --lane canonical_hmac`.
- Validators unchanged: `run_sweep_audit` still executes the full cleanroom, temple, and verification sweeps.
- SKU meaning unchanged: canonical_hmac remains the lawful certification button.
- Added platform tooling only: committed GitHub ruleset desired state, apply/verify helper, posture-consistency extension, and matching tests.
- No runtime-path files for canonical_hmac, router, adapters, or CI doctrine were changed in this sealing commit.
- Clearance result remains a lawful rerun on unchanged canonical_hmac semantics.
