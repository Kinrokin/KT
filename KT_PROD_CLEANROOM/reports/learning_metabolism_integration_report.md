# Learning metabolism integration report

The Academy must feed the current repair loop instead of replacing it.

Required flow:

expanded benchmark failures -> failure taxonomy -> route-regret matrix -> scar clusters -> academy curriculum builder -> crucible/epoch pressure -> targeted lobe repair corpus -> no-regression replay -> benchmark rerun.

This overlay implements prep/shadow contracts for that flow. It does not train, promote, mutate canonical truth, or authorize commercial/superiority claims.
