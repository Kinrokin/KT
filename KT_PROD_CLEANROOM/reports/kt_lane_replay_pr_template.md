# Replay Prep PR: {{ lane_id }}

PREP_ONLY: deterministic replay generated by KT_LANE_COMPILER_V0 as tooling/prep. This scaffold does not authorize runtime, R6 open, package promotion, or commercial claims.

Lane: {{ lane_name }}

Owner: {{ owner }}

Replay scope: regenerate the same prep-only scaffold from the same lane spec and compare deterministic output.

Authority boundary:
- Does not authorize runtime.
- Does not open R6.
- Does not promote packages.
- Does not authorize commercial claims.

Replay checklist:
- Same lane spec input.
- Same generated artifact paths.
- Same generated file bytes.
- Same no-authorization-drift guard values.
