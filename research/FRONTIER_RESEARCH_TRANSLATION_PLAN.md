# Frontier Research Translation

High-value reinforcement -> high-signal repair examples.
Stable substrate / critical initialization -> protect base substrate, learn readouts/adapters first.
Identity mapping / Neuroplex -> record lobe, adapter, route, verifier, gate, receipt IDs per sample.
Wasted effort theory -> spend compute when uncertainty reduction justifies it.
AlphaProof-style verification -> use deterministic extractors, arithmetic checkers, finalizers, and verifier feedback.
Terminal-first direct corpus interaction -> dev agent must inspect repo truth with rg/find/git/cat/sed/head/tail/test outputs.
Observability -> every run emits trace, config, dataset, prompt, scorecard, and experiment receipt.
Noise as resource -> crucible pressure is valuable only when bounded, scored, and mapped to repair.
