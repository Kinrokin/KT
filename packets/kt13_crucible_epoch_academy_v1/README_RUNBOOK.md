# kt13_crucible_epoch_academy_v1

This is a bounded pressure-curriculum evaluation packet. It does not train, promote, authorize production, or create superiority/commercial/external-validation claims.

## Repo Run

Run from repo root:

```bash
python packets/kt13_crucible_epoch_academy_v1/KT13_CRUCIBLE_EPOCH_ACADEMY_V1_RUNNER.py
```

## Kaggle One-Cell Run

Upload:

```text
kt13_crucible_epoch_academy_v1.zip
```

Then paste `KAGGLE_BOOTSTRAP_CELL.py` into one Kaggle cell and run it.

Optional hash pin:

```python
import os
os.environ["KT_ACADEMY_PACKET_SHA256"] = "<packet sha256>"
```

No training, promotion, canonical mutation, or superiority claim is performed.
